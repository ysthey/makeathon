<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.makeathon.telepresenceslave">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
                  android:configChanges="keyboardHidden|orientation|screenSize"
                  android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Translucent">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".roboliterate.activities.RemoteControlActivity"
            android:label="@string/app_name"
            android:logo="@drawable/logo"
            android:background="@color/white"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:configChanges="keyboardHidden|orientation|screenSize"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity android:name=".roboliterate.activities.ChooseDeviceActivity"
                  android:screenOrientation="portrait" android:label="@string/select_device"
                  android:noHistory="true"
                  android:launchMode="singleTop"
                  android:theme="@android:style/Theme.Dialog">
        </activity>
        <activity android:name=".roboliterate.activities.ConfigureDeviceActivity"
                  android:screenOrientation="portrait" android:label="@string/configure_device"
                  android:noHistory="true"
                  android:launchMode="singleTop"
                  android:theme="@android:style/Theme.Dialog">
        </activity>
    </application>

</manifest>